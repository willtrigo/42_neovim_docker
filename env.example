# Alpine Development Environment Configuration
# Copy this file to .env and customize as needed
# cp .env.example .env

# User Configuration
USERNAME=dande-je
USER_UID=1000
USER_GID=1000

# X11 Display Configuration
# Usually :0 on Linux, set automatically on macOS/Windows
DISPLAY=:0

# Git Configuration (optional, override defaults)
# GIT_USER_NAME=dande-je
# GIT_USER_EMAIL=dande-je@student.42sp.org.br

# Neovim Configuration Repository
NVIM_REPO=git@github.com:willtrigo/nvim.config.git

# Container Configuration
CONTAINER_NAME=alpine-dev-workspace
IMAGE_NAME=alpine-dev
IMAGE_TAG=latest

# Volume Names
WORKSPACE_VOLUME=alpine-dev-workspace
NVIM_CONFIG_VOLUME=alpine-dev-nvim-config
HOME_DATA_VOLUME=alpine-dev-home-data

# Alpine Package Manager
# APK is faster than DNF/APT, no additional configuration needed

# Security Options
# If you need additional capabilities for debugging
# ADDITIONAL_CAPS=SYS_ADMIN,NET_ADMIN
